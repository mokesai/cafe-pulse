---
phase: 80-critical-checkout-settings-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/square/process-payment/route.ts
autonomous: true

must_haves:
  truths:
    - "A completed checkout inserts an order row with tenant_id matching the current tenant"
    - "A completed checkout inserts order_item rows with tenant_id matching the current tenant"
    - "The orders INSERT uses createTenantClient so RLS policies apply during insertion"
    - "The auth-only createClient call is preserved for getUser()"
  artifacts:
    - path: "src/app/api/square/process-payment/route.ts"
      provides: "Tenant-scoped order and order_items INSERT"
      contains: "createTenantClient"
  key_links:
    - from: "src/app/api/square/process-payment/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "createTenantClient(tenantId) import"
      pattern: "createTenantClient"
    - from: "tenantSupabase.from('orders').insert"
      to: "orders table RLS policy"
      via: "set_tenant_context RPC inside createTenantClient"
      pattern: "tenant_id.*tenantId"
---

<objective>
Add tenant_id to the orders and order_items INSERT payloads in the checkout payment route, and switch the data-write client from createClient() to createTenantClient(tenantId) so RLS policies apply.

Purpose: Every checkout on the platform silently creates tenant-less orders. A second tenant's order history would be invisible or mix with the default tenant's orders depending on RLS configuration.
Output: process-payment/route.ts with three targeted edits — one new import, one new client variable, and tenant_id added to both INSERT payloads.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-critical-checkout-settings-fixes/80-RESEARCH.md
@src/app/api/square/process-payment/route.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant_id to orders and order_items INSERTs; switch to createTenantClient</name>
  <files>src/app/api/square/process-payment/route.ts</files>
  <action>
Make three targeted edits to src/app/api/square/process-payment/route.ts:

EDIT 1 — Import line (line 5): Add createTenantClient to the existing import from '@/lib/supabase/server'.

Before:
  import { createClient } from '@/lib/supabase/server'

After:
  import { createClient, createTenantClient } from '@/lib/supabase/server'

EDIT 2 — After line 174 (after the getUser() call, before the orders INSERT block starting at line 178): Add a new tenantSupabase client for data writes. Keep the existing `supabase` variable — it is still needed for supabase.auth.getUser() above.

Insert after line 174 (the console.log line for user auth status):
  const tenantSupabase = await createTenantClient(tenantId)

EDIT 3 — Orders INSERT block (lines 178-193): Switch the client from `supabase` to `tenantSupabase` and add `tenant_id: tenantId` as the first field in the insert payload.

Before:
  const { data: orderData, error: orderError } = await supabase
    .from('orders')
    .insert([
      {
        user_id: user?.id || null,
        square_order_id: orderId,
        ...

After:
  const { data: orderData, error: orderError } = await tenantSupabase
    .from('orders')
    .insert([
      {
        tenant_id: tenantId,
        user_id: user?.id || null,
        square_order_id: orderId,
        ...

EDIT 4 — order_items INSERT (lines 219-221): Switch from `supabase` to `tenantSupabase` and add `tenant_id: tenantId` to the map.

The orderItems map (around line 208) should become:
  const orderItems = cartItems.map(item => ({
    tenant_id: tenantId,
    order_id: orderData.id,
    square_item_id: item.id,
    item_name: item.name,
    quantity: item.quantity,
    unit_price: Math.round(item.price * 100),
    total_price: Math.round(item.price * item.quantity * 100),
    variations: item.variationId ? { variationId: item.variationId, variationName: item.variationName } : {},
    modifiers: {}
  }))

  const { error: itemsError } = await tenantSupabase
    .from('order_items')
    .insert(orderItems)

Do NOT change any other part of the file. Do NOT change the supabase.auth.getUser() call or any Square API calls. The tenantId variable is already in scope at line 39 (const tenantId = await getCurrentTenantId()).
  </action>
  <verify>
Run: npm run build
Expected: TypeScript compiles without errors. No type errors on the new createTenantClient import or the tenant_id fields.

Also run: npm run lint
Expected: No new lint errors in process-payment/route.ts.
  </verify>
  <done>
- createTenantClient is imported alongside createClient
- tenantSupabase is created after getUser() using createTenantClient(tenantId)
- orders INSERT uses tenantSupabase and includes tenant_id: tenantId as the first payload field
- order_items map includes tenant_id: tenantId as the first field
- order_items INSERT uses tenantSupabase
- TypeScript build passes with zero errors
  </done>
</task>

</tasks>

<verification>
After the build passes:
1. Confirm grep finds createTenantClient in the file: grep -n "createTenantClient" src/app/api/square/process-payment/route.ts
2. Confirm grep finds tenant_id in both INSERT contexts: grep -n "tenant_id: tenantId" src/app/api/square/process-payment/route.ts — should return two hits (orders payload + orderItems map)
3. Confirm tenantSupabase is used for both INSERTs: grep -n "tenantSupabase" src/app/api/square/process-payment/route.ts — should return three hits (declaration + two INSERT calls)
</verification>

<success_criteria>
- src/app/api/square/process-payment/route.ts imports createTenantClient
- tenantSupabase created after getUser(), before orders INSERT
- orders INSERT: client = tenantSupabase, payload includes tenant_id: tenantId
- order_items map: includes tenant_id: tenantId
- order_items INSERT: client = tenantSupabase
- npm run build exits with code 0
</success_criteria>

<output>
After completion, create .planning/phases/80-critical-checkout-settings-fixes/80-01-SUMMARY.md
</output>
