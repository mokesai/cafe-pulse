---
phase: 85-multi-tenant-schema-constraints
plan: 04
type: execute
wave: 2
depends_on:
  - 85-01
  - 85-02
  - 85-03
files_modified:
  - src/lib/kds/queries.ts
  - src/app/api/admin/cogs/catalog/sync-square/route.ts
  - scripts/seed-cogs-recipes.ts
  - scripts/simulate-cogs-sales.ts
  - scripts/import-kds-menu-from-sheets.js
autonomous: true

must_haves:
  truths:
    - "The COGS catalog sync route successfully upserts cogs_products and cogs_sellables rows when composite constraints are active"
    - "The KDS queries module successfully upserts kds_menu_items, kds_images, and kds_settings rows when composite constraints are active"
    - "The seed-cogs-recipes script successfully upserts cogs_modifier_sets and cogs_modifier_options rows with tenant_id in the payload"
    - "The simulate-cogs-sales script successfully upserts cogs_products and cogs_sellables rows with tenant_id in the payload"
    - "The import-kds-menu-from-sheets script successfully upserts kds_images and kds_settings rows with tenant_id in the payload"
    - "TypeScript build passes with no errors"
  artifacts:
    - path: "src/lib/kds/queries.ts"
      provides: "KDS upsert functions with composite onConflict strings"
      contains: "onConflict: 'tenant_id,square_variation_id'"
    - path: "src/app/api/admin/cogs/catalog/sync-square/route.ts"
      provides: "COGS sync upserts with composite onConflict strings"
      contains: "onConflict: 'tenant_id,square_item_id'"
    - path: "scripts/seed-cogs-recipes.ts"
      provides: "Modifier upserts with tenant_id payload + composite onConflict"
      contains: "onConflict: 'tenant_id,square_modifier_list_id'"
    - path: "scripts/simulate-cogs-sales.ts"
      provides: "COGS upserts with tenant_id payload + composite onConflict"
      contains: "onConflict: 'tenant_id,square_item_id'"
    - path: "scripts/import-kds-menu-from-sheets.js"
      provides: "KDS image/setting upserts with tenant_id payload + composite onConflict"
      contains: "onConflict: 'tenant_id,filename'"
  key_links:
    - from: "src/lib/kds/queries.ts"
      to: "kds_menu_items, kds_images, kds_settings"
      via: "Supabase .upsert() with onConflict composite strings"
      pattern: "onConflict: 'tenant_id,"
    - from: "src/app/api/admin/cogs/catalog/sync-square/route.ts"
      to: "cogs_products, cogs_sellables"
      via: "Supabase .upsert() with onConflict composite strings"
      pattern: "onConflict: 'tenant_id,"
---

<objective>
Update all ON CONFLICT clause strings in application code and scripts to reference the new composite (tenant_id, field) indexes created in plans 85-01/02/03. Add tenant_id to upsert payloads in scripts that were previously omitting it.

Purpose: Without these changes the app will crash immediately after the DDL migrations run — Supabase will fail to find a matching unique constraint for the old single-column onConflict strings. This plan is the app-layer half of GAP-2 remediation.
Output: Five files updated with composite onConflict strings; three scripts also gain tenant_id in their upsert payloads. TypeScript build passes.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-multi-tenant-schema-constraints/85-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update src/ API route and library onConflict strings</name>
  <files>
    src/lib/kds/queries.ts
    src/app/api/admin/cogs/catalog/sync-square/route.ts
  </files>
  <action>
Read both files first, then make the targeted changes described below.

**src/lib/kds/queries.ts**

There are three upsert calls to update (research confirmed these are at approximately lines 341, 368, 396 — read the file to confirm exact lines):

1. `kds_menu_items` upsert — change:
   - `onConflict: 'square_variation_id'` → `onConflict: 'tenant_id,square_variation_id'`

2. `kds_images` upsert — change:
   - `onConflict: 'filename'` → `onConflict: 'tenant_id,filename'`

3. `kds_settings` upsert — change:
   - `onConflict: 'key'` → `onConflict: 'tenant_id,key'`

Do NOT change the `onConflict: 'slug'` call on `kds_categories` — that table is NOT in scope for this phase.
Do NOT add tenant_id to these upsert payloads in queries.ts — the KDS query functions already receive tenantId as a parameter (added in Phase 70-04) and the payloads should already include tenant_id from the call sites.

**src/app/api/admin/cogs/catalog/sync-square/route.ts**

There are two upsert calls to update (research confirmed these are at approximately lines 205, 246 — read the file to confirm exact lines):

1. `cogs_products` upsert — change:
   - `onConflict: 'square_item_id'` → `onConflict: 'tenant_id,square_item_id'`

2. `cogs_sellables` upsert — change:
   - `onConflict: 'square_variation_id'` → `onConflict: 'tenant_id,square_variation_id'`

Verify that tenant_id is already present in the upsert row payloads. If not, add `tenant_id: tenantId` to each row object (tenantId should already be available from getCurrentTenantId() at the top of the route — see Phase 70-06).

After editing both files, run `npm run build` or `npx tsc --noEmit` to confirm TypeScript has no errors.
  </action>
  <verify>
Run `npx tsc --noEmit` and confirm zero TypeScript errors.

Confirm the changes are in place:
```bash
grep -n "onConflict" /Users/jerrym/Documents/projects/KP3-cafe/cafe-web/website/src/lib/kds/queries.ts
```
Expected output includes lines with `'tenant_id,square_variation_id'`, `'tenant_id,filename'`, and `'tenant_id,key'`. Must NOT show bare `'square_variation_id'`, `'filename'`, or `'key'` (without tenant_id prefix) for those three calls.

```bash
grep -n "onConflict" /Users/jerrym/Documents/projects/KP3-cafe/cafe-web/website/src/app/api/admin/cogs/catalog/sync-square/route.ts
```
Expected output includes `'tenant_id,square_item_id'` and `'tenant_id,square_variation_id'`.
  </verify>
  <done>
Both files updated. `queries.ts` has three composite onConflict strings (kds_menu_items, kds_images, kds_settings). `sync-square/route.ts` has two composite onConflict strings (cogs_products, cogs_sellables). TypeScript build passes with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update scripts onConflict strings and add tenant_id to payloads</name>
  <files>
    scripts/seed-cogs-recipes.ts
    scripts/simulate-cogs-sales.ts
    scripts/import-kds-menu-from-sheets.js
  </files>
  <action>
Read all three files first. These scripts were NOT updated in Phase 70 and currently omit tenant_id from their upsert payloads. After the DDL migrations, `ON CONFLICT(tenant_id, field)` will fail if tenant_id is absent from the row — PostgreSQL will reject the upsert entirely.

Each script must have TWO changes per affected upsert: (1) update the onConflict string, and (2) ensure tenant_id is in the upsert payload.

**scripts/seed-cogs-recipes.ts** (research confirmed ~lines 565, 578):

1. `cogs_modifier_sets` upsert (around line 565):
   - Change `onConflict: 'square_modifier_list_id'` → `onConflict: 'tenant_id,square_modifier_list_id'`
   - Add `tenant_id` to the upsert row object. The script must have a tenantId variable available — look for how it resolves the tenant (likely via command-line flag or default tenant UUID `00000000-0000-0000-0000-000000000001`). Add `tenant_id: tenantId` (or the equivalent constant) to each row in the upsert payload.

2. `cogs_modifier_options` upsert (around line 578):
   - Change `onConflict: 'square_modifier_id'` → `onConflict: 'tenant_id,square_modifier_id'`
   - Add `tenant_id: tenantId` to each row in the upsert payload (same tenantId source as above).

**scripts/simulate-cogs-sales.ts** (research confirmed ~lines 424, 450):

1. `cogs_products` upsert (around line 424):
   - Change `onConflict: 'square_item_id'` → `onConflict: 'tenant_id,square_item_id'`
   - Add `tenant_id: tenantId` to each row in the upsert payload.

2. `cogs_sellables` upsert (around line 450):
   - Change `onConflict: 'square_variation_id'` → `onConflict: 'tenant_id,square_variation_id'`
   - Add `tenant_id: tenantId` to each row in the upsert payload.

**scripts/import-kds-menu-from-sheets.js** (research confirmed ~lines 483, 507):

1. `kds_images` upsert (around line 483):
   - Change `onConflict: 'filename'` → `onConflict: 'tenant_id,filename'`
   - Add `tenant_id` to the object returned by `transformImage()` (or wherever kds_images rows are constructed). Use the default tenant UUID `'00000000-0000-0000-0000-000000000001'` if the script does not already resolve a tenant.

2. `kds_settings` upsert (around line 507):
   - Change `onConflict: 'key'` → `onConflict: 'tenant_id,key'`
   - Add `tenant_id` to the object returned by `transformSetting()` (or wherever kds_settings rows are constructed). Use the same tenant_id approach as kds_images above.

Do NOT change the `onConflict: 'slug'` call on `kds_categories` in import-kds-menu-from-sheets.js (around line 337) — that table is not in scope.

After editing all three files, run `npx tsc --noEmit` to confirm no TypeScript errors (this covers the .ts files; .js file is not type-checked but verify it has no obvious syntax errors).
  </action>
  <verify>
```bash
grep -n "onConflict" /Users/jerrym/Documents/projects/KP3-cafe/cafe-web/website/scripts/seed-cogs-recipes.ts
```
Expected: `'tenant_id,square_modifier_list_id'` and `'tenant_id,square_modifier_id'`.

```bash
grep -n "onConflict" /Users/jerrym/Documents/projects/KP3-cafe/cafe-web/website/scripts/simulate-cogs-sales.ts
```
Expected: `'tenant_id,square_item_id'` and `'tenant_id,square_variation_id'`.

```bash
grep -n "onConflict" /Users/jerrym/Documents/projects/KP3-cafe/cafe-web/website/scripts/import-kds-menu-from-sheets.js
```
Expected: `'tenant_id,filename'` and `'tenant_id,key'`. Must NOT show bare `'filename'` or `'key'` without tenant_id prefix.

Run `npx tsc --noEmit` — zero TypeScript errors expected.
  </verify>
  <done>
All three scripts updated with composite onConflict strings and tenant_id added to upsert payloads. TypeScript build passes with no errors.
  </done>
</task>

</tasks>

<verification>
- `src/lib/kds/queries.ts`: onConflict strings updated for kds_menu_items, kds_images, kds_settings; kds_categories unchanged
- `src/app/api/admin/cogs/catalog/sync-square/route.ts`: onConflict strings updated for cogs_products, cogs_sellables
- `scripts/seed-cogs-recipes.ts`: onConflict strings updated, tenant_id added to modifier_sets and modifier_options payloads
- `scripts/simulate-cogs-sales.ts`: onConflict strings updated, tenant_id added to cogs_products and cogs_sellables payloads
- `scripts/import-kds-menu-from-sheets.js`: onConflict strings updated, tenant_id added to kds_images and kds_settings rows
- `npx tsc --noEmit` passes with zero errors
- No admin UI error handling changes needed (research confirmed no constraint-name string references in src/)
</verification>

<success_criteria>
All five files have composite onConflict strings that match the new composite indexes created in 85-01/02/03. Scripts include tenant_id in upsert payloads. TypeScript build clean. Phase 85 GAP-2 remediation complete.
</success_criteria>

<output>
After completion, create `.planning/phases/85-multi-tenant-schema-constraints/85-04-SUMMARY.md`
</output>
