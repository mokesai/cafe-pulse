---
phase: 96-tenant-resolution-hardening-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "SQUARE_SECRET env var is documented in CLAUDE.md Environment Setup section"
    - "Documentation clearly identifies SQUARE_SECRET as the OAuth application secret, not access token"
    - "SQUARE_SECRET description notes it's required for OAuth callback route"
  artifacts:
    - path: CLAUDE.md
      provides: "SQUARE_SECRET in environment variables list"
      contains: "SQUARE_SECRET"
      min_lines: 100
  key_links:
    - from: CLAUDE.md env var documentation
      to: src/app/api/platform/square-oauth/callback/route.ts
      via: "developer reads env var docs, sets SQUARE_SECRET, OAuth callback works"
      pattern: "SQUARE_SECRET"
---

<objective>
Close Finding 5 from v1.0 audit — document the SQUARE_SECRET environment variable.

Purpose: Operational completeness — developers and platform admins need to know that SQUARE_SECRET is required for the Square OAuth flow to work. Without documentation, the OAuth callback fails with a cryptic "oauth_not_configured" error.

Output: CLAUDE.md updated with SQUARE_SECRET in the Required .env.local variables section.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-tenant-resolution-hardening-documentation/96-CONTEXT.md
@.planning/phases/96-tenant-resolution-hardening-documentation/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add SQUARE_SECRET to CLAUDE.md environment variables</name>
  <files>CLAUDE.md</files>
  <action>
Update the "Environment Setup" section in CLAUDE.md (around line 125-131) to add SQUARE_SECRET to the Square environment variables list.

Current Square env vars entry (line ~128):
```
- `SQUARE_APPLICATION_ID` / `SQUARE_ACCESS_TOKEN` / `SQUARE_LOCATION_ID` / `SQUARE_ENVIRONMENT` — Square
```

Update to expanded format for clarity:
```
- `SQUARE_APPLICATION_ID` — Square application ID
- `SQUARE_ACCESS_TOKEN` — Square API access token
- `SQUARE_LOCATION_ID` — Square location ID
- `SQUARE_ENVIRONMENT` — `sandbox` or `production`
- `SQUARE_SECRET` — Square OAuth application secret (required for `/api/platform/square-oauth/callback`)
```

Why this description:
- "OAuth application secret" — distinguishes it from SQUARE_ACCESS_TOKEN
- "required for `/api/platform/square-oauth/callback`" — tells developer when they need it
- Found in Square Developer Dashboard under your application's OAuth settings tab

What NOT to call it:
- NOT "webhook secret" (that's SQUARE_WEBHOOK_SIGNATURE_KEY)
- NOT "access token secret" (confusing with SQUARE_ACCESS_TOKEN)
- NOT just "Square secret" (too vague)

Research evidence: callback/route.ts uses SQUARE_SECRET as client_secret in the OAuth authorization code exchange (line 93). It's the OAuth 2.0 client_secret parameter, not a webhook or API secret.
  </action>
  <verify>grep -n "SQUARE_SECRET" CLAUDE.md</verify>
  <done>CLAUDE.md Environment Setup section lists SQUARE_SECRET with description indicating it's the OAuth application secret required for the callback route</done>
</task>

</tasks>

<verification>
After completion:

1. SQUARE_SECRET documented:
   ```bash
   grep -A2 "SQUARE_SECRET" CLAUDE.md
   ```

2. Entry appears in Environment Setup section (not elsewhere):
   ```bash
   grep -B10 "SQUARE_SECRET" CLAUDE.md | grep "Environment Setup"
   ```

3. Description is clear and distinguishes it from SQUARE_ACCESS_TOKEN:
   ```bash
   grep "OAuth application secret" CLAUDE.md
   ```
</verification>

<success_criteria>
- SQUARE_SECRET appears in CLAUDE.md Environment Setup section
- Description clearly identifies it as OAuth application secret
- Description mentions it's required for the OAuth callback route
- Format matches other env var entries in the same section
</success_criteria>

<output>
After completion, create `.planning/phases/96-tenant-resolution-hardening-documentation/96-02-SUMMARY.md`
</output>
