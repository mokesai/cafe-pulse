---
phase: 50-tenant-auth-identity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260215140000_add_tenant_branding_columns.sql
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Database has logo_url, primary_color, secondary_color columns in tenants table"
    - "getTenantIdentity() successfully loads branding data without errors"
    - "Site loads without 500 errors on all pages"
  artifacts:
    - path: "supabase/migrations/20260215140000_add_tenant_branding_columns.sql"
      provides: "Migration adding branding columns to tenants table"
      min_lines: 20
  key_links:
    - from: "src/lib/tenant/identity.ts"
      to: "tenants table columns"
      via: "SELECT query lines 27-33"
      pattern: "logo_url, primary_color, secondary_color"
---

<objective>
Close UAT gap: Add missing branding columns (logo_url, primary_color, secondary_color) to tenants table to fix 500 errors on all pages.

Purpose: Enable per-tenant UI customization that code expects but database schema is missing. This was deferred from Phase 50-01 but never implemented.
Output: Database migration that adds three nullable branding columns to tenants table, allowing getTenantIdentity() to load successfully.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/tenant/types.ts
@src/lib/tenant/identity.ts
@.planning/phases/50-tenant-auth-identity/50-01-SUMMARY.md
@.planning/phases/50-tenant-auth-identity/50-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Create migration to add branding columns to tenants table</name>
  <files>supabase/migrations/20260215140000_add_tenant_branding_columns.sql</files>
  <action>
Create migration file `20260215140000_add_tenant_branding_columns.sql` that:

1. Adds three nullable columns to public.tenants table:
   - logo_url (text) - URL to tenant logo image
   - primary_color (text) - Hex color for branding (e.g., #f59e0b)
   - secondary_color (text) - Secondary hex color for branding

2. Uses idempotent pattern: `ADD COLUMN IF NOT EXISTS`

3. Optionally sets default values for the default tenant (id = '00000000-0000-0000-0000-000000000001'):
   - primary_color: '#f59e0b' (Little Cafe's amber brand color)
   - secondary_color: '#0f172a' (dark slate for text)
   - logo_url: null (to be configured later)

4. Follows existing migration conventions from supabase/migrations/ directory

Migration structure:
```sql
-- Migration: Add tenant branding columns
-- Phase 50, Plan 06: Gap closure for missing branding schema

-- Add branding columns to tenants table
ALTER TABLE public.tenants
  ADD COLUMN IF NOT EXISTS logo_url text,
  ADD COLUMN IF NOT EXISTS primary_color text,
  ADD COLUMN IF NOT EXISTS secondary_color text;

-- Set default branding for default tenant (Little Cafe)
UPDATE public.tenants
SET
  primary_color = '#f59e0b',
  secondary_color = '#0f172a'
WHERE id = '00000000-0000-0000-0000-000000000001'
  AND primary_color IS NULL; -- Only if not already set
```

Migration comment should reference Phase 50, Plan 06 and explain this closes the UAT gap from 50-01's deferred schema work.
  </action>
  <verify>
1. Migration file exists at correct path with timestamp 20260215140000
2. File contains ALTER TABLE with three ADD COLUMN IF NOT EXISTS statements
3. File contains UPDATE for default tenant with WHERE conditions
4. Migration follows naming convention from other files in supabase/migrations/
  </verify>
  <done>
Migration file created with:
- Three branding columns added with nullable constraint
- Default tenant gets Little Cafe brand colors
- Idempotent pattern ensures safe re-runs
- Migration ready to apply to database
  </done>
</task>

<task type="auto">
  <name>Apply migration to database</name>
  <files>supabase/migrations/20260215140000_add_tenant_branding_columns.sql</files>
  <action>
Apply the migration to the development Supabase database using the npm script:

```bash
npm run db:migrate
```

This runs `supabase db push` which applies all pending migrations in supabase/migrations/ directory to the linked Supabase project.

IMPORTANT: Verify `.env.local` points to the correct Supabase project before running. Development should use `ofppjltowsdvojixeflr`.

After migration applies successfully, verify the columns exist:
1. Check Supabase dashboard table editor for tenants table
2. Verify three new columns appear: logo_url, primary_color, secondary_color
3. Verify default tenant row has primary_color='#f59e0b' and secondary_color='#0f172a'
  </action>
  <verify>
1. `npm run db:migrate` completes successfully with no errors
2. Terminal output shows migration 20260215140000 applied
3. Supabase logs show ALTER TABLE and UPDATE statements executed
4. Development server can start without 500 errors
5. getTenantIdentity() query succeeds (test by loading any page)
  </verify>
  <done>
Migration applied to database:
- Tenants table has three new branding columns
- Default tenant has Little Cafe brand colors set
- getTenantIdentity() query succeeds without column errors
- Site loads without 500 errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Database migration adding logo_url, primary_color, secondary_color columns to tenants table. Applied to dev database and default tenant populated with Little Cafe brand colors.
  </what-built>
  <how-to-verify>
1. **Start dev server** (if not running):
   ```bash
   npm run dev:webpack
   ```

2. **Visit customer-facing page**:
   - Navigate to http://littlecafe.localhost:3000
   - Page should load successfully (not 500 error)
   - Check terminal for getTenantIdentity() success

3. **Visit admin panel**:
   - Navigate to http://littlecafe.localhost:3000/admin
   - Admin panel should load successfully
   - No "column tenants.logo_url does not exist" errors

4. **Verify database schema** (optional):
   - Open Supabase dashboard: https://supabase.com/dashboard/project/ofppjltowsdvojixeflr
   - Navigate to Table Editor → tenants
   - Confirm columns exist: logo_url (text), primary_color (text), secondary_color (text)
   - Check default tenant row has primary_color = '#f59e0b'

5. **Check for console errors**:
   - Browser console should have no React/Next.js errors related to tenant identity
   - Terminal should show no database query errors

Expected outcome: All pages load successfully, getTenantIdentity() succeeds, 500 errors resolved.
  </how-to-verify>
  <resume-signal>
Type "approved" if site loads successfully without errors, or describe any remaining issues.
  </resume-signal>
</task>

</tasks>

<verification>
## Overall Phase Checks

After all tasks complete:

1. **Schema verification**:
   - [ ] Tenants table has logo_url column (text, nullable)
   - [ ] Tenants table has primary_color column (text, nullable)
   - [ ] Tenants table has secondary_color column (text, nullable)
   - [ ] Default tenant has brand colors set

2. **Code verification**:
   - [ ] getTenantIdentity() query at identity.ts:27-33 succeeds
   - [ ] No TypeScript errors referencing branding fields
   - [ ] TenantPublic type includes branding fields (types.ts:13-15)

3. **Runtime verification**:
   - [ ] Customer pages load without 500 errors
   - [ ] Admin pages load without 500 errors
   - [ ] Terminal shows no "column does not exist" errors
   - [ ] Browser console shows no React errors

4. **UAT gap closure**:
   - [ ] Test 1 from 50-UAT.md now passes (getTenantIdentity() loads branding)
   - [ ] Site fully functional on default tenant
   - [ ] Ready to configure per-tenant branding in Phase 60
</verification>

<success_criteria>
Gap closure complete when:
- Migration file exists and follows conventions
- Migration applied successfully to dev database
- Tenants table has three branding columns
- Default tenant has Little Cafe colors set
- getTenantIdentity() succeeds without errors
- Site loads on both customer and admin routes
- UAT Test 1 failure resolved (14/14 tests passing)
- No remaining 500 errors on any pages
</success_criteria>

<output>
After completion, create `.planning/phases/50-tenant-auth-identity/50-06-SUMMARY.md` documenting:
- Migration created and applied
- Three columns added: logo_url, primary_color, secondary_color
- Default tenant branding configured
- UAT gap closed: Test 1 now passes
- Site operational with tenant branding schema in place
</output>
