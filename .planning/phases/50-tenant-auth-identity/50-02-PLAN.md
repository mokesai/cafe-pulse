---
phase: 50-tenant-auth-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/email/templates/OrderConfirmation.tsx
  - src/lib/email/templates/OrderStatusUpdate.tsx
autonomous: true

must_haves:
  truths:
    - React Email installed with @react-email/components
    - Email templates exist as React components (not HTML strings)
    - Templates accept tenant branding props (businessName, primaryColor, etc)
  artifacts:
    - path: package.json
      provides: react-email dependencies
      contains: "react-email"
    - path: src/lib/email/templates/OrderConfirmation.tsx
      provides: Order confirmation email template component
      min_lines: 50
      exports: [default]
    - path: src/lib/email/templates/OrderStatusUpdate.tsx
      provides: Order status update email template component
      min_lines: 40
      exports: [default]
  key_links:
    - from: src/lib/email/templates/OrderConfirmation.tsx
      to: "@react-email/components"
      via: Import Html, Container, Heading components
      pattern: "from '@react-email/components'"
---

<objective>
Install React Email and create foundational email template components with tenant branding support.

Purpose: Replace HTML string email templates with type-safe React components that accept tenant branding props for multi-tenant customization.

Output: React Email templates for order confirmations and status updates, ready to be integrated with tenant identity system.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-tenant-auth-identity/50-RESEARCH.md
@.planning/50-CONTEXT.md

@src/lib/email/service.ts
</context>

<tasks>

<task type="auto">
  <name>Install React Email dependencies</name>
  <files>package.json</files>
  <action>
  Install React Email and its components:

  ```bash
  npm install react-email @react-email/components
  npm install -D @react-email/render
  ```

  React Email provides type-safe email components that render to HTML with cross-client compatibility. @react-email/render converts React components to HTML strings for Resend.

  Verify installation completes without errors.
  </action>
  <verify>package.json contains react-email, @react-email/components, @react-email/render; npm install completes successfully</verify>
  <done>React Email dependencies installed and locked in package-lock.json</done>
</task>

<task type="auto">
  <name>Create OrderConfirmation React Email template</name>
  <files>src/lib/email/templates/OrderConfirmation.tsx</files>
  <action>
  Create new file src/lib/email/templates/OrderConfirmation.tsx with React Email template following research pattern:

  ```typescript
  import {
    Html, Head, Body, Container, Heading, Text, Section, Hr
  } from '@react-email/components'

  interface OrderConfirmationProps {
    // Order data
    orderId: string
    customerName: string
    items: Array<{
      name: string
      quantity: number
      price: number
      total: number
    }>
    subtotal: number
    tax: number
    total: number
    pickupTime?: string
    specialInstructions?: string

    // Tenant branding (will be populated from getTenantIdentity)
    businessName: string
    businessAddress?: string
    businessPhone?: string
    businessEmail?: string
    businessHours?: string
    logoUrl?: string
    primaryColor?: string
  }

  export default function OrderConfirmation({
    orderId,
    customerName,
    items,
    subtotal,
    tax,
    total,
    pickupTime,
    specialInstructions,
    businessName,
    businessAddress,
    businessPhone,
    businessEmail,
    businessHours,
    primaryColor = '#f59e0b',
  }: OrderConfirmationProps) {
    return (
      <Html>
        <Head />
        <Body style={{ fontFamily: 'Arial, sans-serif', lineHeight: '1.6', color: '#333' }}>
          <Container style={{ maxWidth: '600px', margin: '0 auto', padding: '20px' }}>
            <Section style={{ background: primaryColor, color: 'white', padding: '20px', textAlign: 'center', borderRadius: '8px 8px 0 0' }}>
              <Heading style={{ margin: 0 }}>Order Confirmation</Heading>
              <Text style={{ margin: '10px 0 0' }}>Thank you for your order, {customerName}!</Text>
            </Section>

            <Section style={{ background: '#f9f9f9', padding: '20px', borderRadius: '0 0 8px 8px' }}>
              <Section style={{ background: 'white', padding: '15px', borderRadius: '6px', margin: '15px 0' }}>
                <Heading as="h3" style={{ margin: '0 0 10px' }}>Order #{orderId.slice(-8)}</Heading>

                <Heading as="h4" style={{ margin: '15px 0 10px' }}>Items Ordered:</Heading>
                {items.map((item, idx) => (
                  <Section key={idx} style={{ display: 'flex', justifyContent: 'space-between', padding: '8px 0', borderBottom: '1px solid #eee' }}>
                    <Text style={{ margin: 0 }}>{item.quantity}x {item.name}</Text>
                    <Text style={{ margin: 0 }}>${item.total.toFixed(2)}</Text>
                  </Section>
                ))}

                <Hr style={{ margin: '15px 0', border: 'none', borderTop: `2px solid ${primaryColor}` }} />

                <Section style={{ display: 'flex', justifyContent: 'space-between', padding: '8px 0' }}>
                  <Text style={{ margin: 0 }}>Subtotal:</Text>
                  <Text style={{ margin: 0 }}>${subtotal.toFixed(2)}</Text>
                </Section>
                <Section style={{ display: 'flex', justifyContent: 'space-between', padding: '8px 0' }}>
                  <Text style={{ margin: 0 }}>Tax:</Text>
                  <Text style={{ margin: 0 }}>${tax.toFixed(2)}</Text>
                </Section>
                <Section style={{ display: 'flex', justifyContent: 'space-between', padding: '8px 0', fontWeight: 'bold' }}>
                  <Text style={{ margin: 0 }}>Total:</Text>
                  <Text style={{ margin: 0 }}>${total.toFixed(2)}</Text>
                </Section>

                {specialInstructions && (
                  <Section style={{ marginTop: '15px' }}>
                    <Text style={{ fontWeight: 'bold', margin: '0 0 5px' }}>Special Instructions:</Text>
                    <Text style={{ margin: 0 }}>{specialInstructions}</Text>
                  </Section>
                )}
              </Section>

              <Section style={{ background: '#fef3c7', padding: '15px', borderRadius: '6px', margin: '15px 0' }}>
                <Heading as="h4" style={{ margin: '0 0 10px' }}>Pickup Location:</Heading>
                <Text style={{ margin: 0, fontWeight: 'bold' }}>{businessName}</Text>
                {businessAddress && <Text style={{ margin: '5px 0 0' }}>{businessAddress}</Text>}
                {businessHours && <Text style={{ margin: '5px 0 0' }}>Hours: {businessHours}</Text>}
                {pickupTime && (
                  <Text style={{ margin: '10px 0 0', fontWeight: 'bold' }}>
                    Estimated Pickup Time: {pickupTime}
                  </Text>
                )}
              </Section>

              <Text>We'll send you another email when your order is ready for pickup!</Text>
            </Section>

            <Section style={{ textAlign: 'center', marginTop: '20px', color: '#666', fontSize: '14px' }}>
              {businessEmail && <Text style={{ margin: '5px 0' }}>Questions? Contact us at {businessEmail}</Text>}
              <Text style={{ margin: '5px 0' }}>{businessName} - Fresh coffee, made with care</Text>
            </Section>
          </Container>
        </Body>
      </Html>
    )
  }
  ```

  Template uses React Email components for cross-client compatibility. Accepts tenant branding props (businessName, primaryColor, etc) that will be populated from getTenantIdentity(). Uses same visual structure as existing HTML template for consistency.
  </action>
  <verify>TypeScript build passes; template file compiles without errors</verify>
  <done>OrderConfirmation React Email template exists with tenant branding props</done>
</task>

<task type="auto">
  <name>Create OrderStatusUpdate React Email template</name>
  <files>src/lib/email/templates/OrderStatusUpdate.tsx</files>
  <action>
  Create src/lib/email/templates/OrderStatusUpdate.tsx:

  ```typescript
  import {
    Html, Head, Body, Container, Heading, Text, Section
  } from '@react-email/components'

  interface OrderStatusUpdateProps {
    orderId: string
    status: string
    message: string
    customerName?: string

    // Tenant branding
    businessName: string
    businessAddress?: string
    businessPhone?: string
    businessEmail?: string
    businessHours?: string
    primaryColor?: string
  }

  const statusColors: Record<string, string> = {
    confirmed: '#10b981',
    preparing: '#f59e0b',
    ready: '#059669',
    completed: '#6b7280',
    cancelled: '#ef4444'
  }

  export default function OrderStatusUpdate({
    orderId,
    status,
    message,
    customerName,
    businessName,
    businessAddress,
    businessPhone,
    businessEmail,
    businessHours,
    primaryColor
  }: OrderStatusUpdateProps) {
    const color = statusColors[status] || primaryColor || '#6b7280'

    return (
      <Html>
        <Head />
        <Body style={{ fontFamily: 'Arial, sans-serif', lineHeight: '1.6', color: '#333' }}>
          <Container style={{ maxWidth: '600px', margin: '0 auto', padding: '20px' }}>
            <Section style={{ background: color, color: 'white', padding: '20px', textAlign: 'center', borderRadius: '8px 8px 0 0' }}>
              <Heading style={{ margin: 0 }}>Order Update</Heading>
              {customerName && <Text style={{ margin: '10px 0 0' }}>Hi {customerName}!</Text>}
            </Section>

            <Section style={{ background: '#f9f9f9', padding: '20px', borderRadius: '0 0 8px 8px' }}>
              <Text style={{ fontWeight: 'bold' }}>Order #{orderId.slice(-8)}</Text>

              <Section style={{ textAlign: 'center', margin: '20px 0' }}>
                <span style={{
                  background: color,
                  color: 'white',
                  padding: '8px 16px',
                  borderRadius: '20px',
                  display: 'inline-block',
                  fontWeight: 'bold',
                  textTransform: 'uppercase'
                }}>
                  {status}
                </span>
              </Section>

              <Text>{message}</Text>

              {status === 'ready' && (
                <Section style={{ background: '#fef3c7', padding: '15px', borderRadius: '6px', margin: '15px 0' }}>
                  <Heading as="h4" style={{ margin: '0 0 10px' }}>Pickup Location:</Heading>
                  <Text style={{ margin: 0, fontWeight: 'bold' }}>{businessName}</Text>
                  {businessAddress && <Text style={{ margin: '5px 0 0' }}>{businessAddress}</Text>}
                  {businessHours && <Text style={{ margin: '5px 0 0' }}>Hours: {businessHours}</Text>}
                </Section>
              )}
            </Section>

            <Section style={{ textAlign: 'center', marginTop: '20px', color: '#666', fontSize: '14px' }}>
              {businessEmail && <Text style={{ margin: '5px 0' }}>Questions? Contact us at {businessEmail}</Text>}
              <Text style={{ margin: '5px 0' }}>{businessName} - Fresh coffee, made with care</Text>
            </Section>
          </Container>
        </Body>
      </Html>
    )
  }
  ```

  Template handles different status colors. Shows pickup location for "ready" status. Accepts tenant branding props for customization.
  </action>
  <verify>TypeScript build passes; template renders without errors</verify>
  <done>OrderStatusUpdate React Email template exists with status-aware styling and tenant branding</done>
</task>

</tasks>

<verification>
Run TypeScript build:
```bash
npm run build
```

Verify templates import correctly:
```typescript
import OrderConfirmation from '@/lib/email/templates/OrderConfirmation'
import OrderStatusUpdate from '@/lib/email/templates/OrderStatusUpdate'
```
</verification>

<success_criteria>
- [ ] react-email and @react-email/components installed
- [ ] @react-email/render installed as dev dependency
- [ ] OrderConfirmation.tsx exists with tenant branding props
- [ ] OrderStatusUpdate.tsx exists with status-aware colors
- [ ] Both templates use React Email components (Html, Container, etc)
- [ ] TypeScript build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/50-tenant-auth-identity/50-02-SUMMARY.md`
</output>
