---
phase: 70-integration-testing-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/webhooks/square/catalog/route.ts
  - src/app/api/webhooks/square/inventory/route.ts
  - src/lib/kds/queries.ts
  - src/lib/supabase/database.ts
  - src/lib/admin/setup.ts
  - src/app/api/orders/route.ts
  - src/app/api/admin/kds/settings/route.ts
  - src/app/admin/(kds)/layout.tsx
  - src/app/kds/layout.tsx
  - src/app/admin/(kds)/kds/food/page.tsx
  - src/app/admin/(kds)/kds/drinks/page.tsx
  - src/app/admin/(protected)/settings/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Catalog webhook only touches inventory_items, suppliers, and webhook_events rows belonging to the resolved tenant"
    - "Inventory webhook only touches inventory_items, stock_movements, low_stock_alerts, and webhook_events rows belonging to the resolved tenant"
    - "KDS query functions accept a tenantId parameter and filter all DB queries by tenant_id"
    - "database.ts createOrder() passes tenant_id on order and order_items inserts"
    - "api/orders/route.ts calls getCurrentTenantId() and passes tenantId to createOrder()"
    - "admin/setup.ts profile functions are replaced or scoped to not use bare service-role queries on profiles"
    - "All 6 KDS caller files pass tenantId to every kds/queries function call"
  artifacts:
    - path: "src/app/api/webhooks/square/catalog/route.ts"
      provides: "Catalog webhook with tenant-scoped DB queries"
      contains: ".eq('tenant_id', tenantId)"
    - path: "src/app/api/webhooks/square/inventory/route.ts"
      provides: "Inventory webhook with tenant-scoped DB queries"
      contains: ".eq('tenant_id', tenantId)"
    - path: "src/lib/kds/queries.ts"
      provides: "KDS queries parameterized by tenantId"
      exports: ["getCategories", "getMenuItems", "getImages", "getSettings", "getScreenData"]
    - path: "src/lib/supabase/database.ts"
      provides: "createOrder with tenant_id"
      contains: "tenant_id: tenantId"
    - path: "src/lib/admin/setup.ts"
      provides: "Admin setup with tenant-scoped profile lookup"
      contains: "tenant_id"
    - path: "src/app/api/orders/route.ts"
      provides: "Order creation route passing tenantId to createOrder"
      contains: "getCurrentTenantId"
  key_links:
    - from: "src/app/api/webhooks/square/catalog/route.ts"
      to: "resolveTenantFromMerchantId"
      via: "existing import from @/lib/square/config"
      pattern: "resolveTenantFromMerchantId"
    - from: "src/lib/kds/queries.ts"
      to: "kds_categories, kds_menu_items, kds_images, kds_settings"
      via: "tenantId parameter passed to .eq('tenant_id', tenantId)"
      pattern: "\\.eq\\('tenant_id', tenantId\\)"
    - from: "src/app/api/orders/route.ts"
      to: "createOrder"
      via: "getCurrentTenantId() from @/lib/tenant/context"
      pattern: "getCurrentTenantId"
---

<objective>
Close the CRITICAL and HIGH service-role query gaps identified in the 70-02 security audit. The 6 highest-risk files use createServiceClient() to bypass RLS but do not pass tenant_id filtering, enabling cross-tenant data leakage. Also update all callers of kds/queries.ts and database.ts createOrder() so the TypeScript build remains clean after signature changes.

Purpose: The phase goal — "audit all service-role queries for explicit tenant filtering" — requires these queries to be fixed, not just documented. Webhooks are CRITICAL because they process events from ALL tenants with no user context; a missed filter would write to the wrong tenant's data silently.

Output: 5 library/route files updated with tenant-scoped DB queries, plus 7 caller files updated to pass tenantId. Audit re-run will show 5 fewer FAIL findings. Build stays clean.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-integration-testing-hardening/70-VERIFICATION.md
@audits/AUDIT_RESULTS.md
@src/lib/tenant/context.ts
@src/app/api/webhooks/square/catalog/route.ts
@src/app/api/webhooks/square/inventory/route.ts
@src/lib/kds/queries.ts
@src/lib/supabase/database.ts
@src/lib/admin/setup.ts
@src/app/api/orders/route.ts
@src/app/api/admin/kds/settings/route.ts
@src/app/admin/(kds)/layout.tsx
@src/app/kds/layout.tsx
@src/app/admin/(kds)/kds/food/page.tsx
@src/app/admin/(kds)/kds/drinks/page.tsx
@src/app/admin/(protected)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant_id filtering to both Square webhook routes</name>
  <files>
    src/app/api/webhooks/square/catalog/route.ts
    src/app/api/webhooks/square/inventory/route.ts
  </files>
  <action>
    Both webhook routes already resolve tenantId via resolveTenantFromMerchantId() at the top of POST(). The fix is to pass that tenantId down to every helper function and add .eq('tenant_id', tenantId) to every .from() query.

    For catalog/route.ts:
    1. Change getLastCatalogSync() to accept tenantId: string parameter. Add .eq('tenant_id', tenantId) to the webhook_events SELECT query inside it.
    2. Change syncCatalogChanges() to accept tenantId: string as its first parameter. Add .eq('tenant_id', tenantId) to: inventory_items SELECT query, suppliers SELECT query, inventory_items UPDATE query, inventory_items INSERT (add tenant_id field to newInventoryItem object as `tenant_id: tenantId`).
    3. Change logWebhookEvent() to accept tenantId: string. Add `tenant_id: tenantId` to the webhook_events INSERT payload.
    4. In the main POST handler: update calls to getLastCatalogSync(tenantId), syncCatalogChanges(catalogData, tenantId) [parameter order: tenantId first for consistency], logWebhookEvent(event, syncResult, tenantId). Also add .eq('tenant_id', tenantId) to the deduplication check query (the webhook_events SELECT .eq('event_id', event.event_id)).

    For inventory/route.ts:
    1. Change getInventoryItemBySquareId() to accept tenantId: string. Add .eq('tenant_id', tenantId) to the inventory_items SELECT.
    2. Change updateInventoryStock() to accept tenantId: string. Add .eq('tenant_id', tenantId) to the inventory_items UPDATE. Add tenant_id: tenantId to the stock_movements INSERT payload.
    3. Change checkLowStockAlert() to accept tenantId: string. Add .eq('tenant_id', tenantId) to the low_stock_alerts SELECT (check existing alert). Add tenant_id: tenantId to the low_stock_alerts INSERT payload.
    4. Change processInventoryUpdates() to accept tenantId: string as a new parameter (after locationId). Thread tenantId through calls to getInventoryItemBySquareId(count.catalog_object_id, tenantId) and updateInventoryStock(..., tenantId). Thread tenantId through to checkLowStockAlert(inventoryItem, newQuantity, tenantId).
    5. Change logWebhookEvent() to accept tenantId: string. Add tenant_id: tenantId to the webhook_events INSERT payload.
    6. In the main POST handler: add .eq('tenant_id', tenantId) to the deduplication check query. Update calls to processInventoryUpdates(squareConfig.locationId, ..., tenantId) and logWebhookEvent(event, processResult, tenantId).

    Do NOT change the GET handler or any logic that does not touch the database.
    Do NOT change verifySquareSignature, fetchCatalogChanges, makeSquareHeaders, or generateInventoryDefaults — they have no DB access.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Should produce zero TypeScript errors in both webhook files.
    Run: grep -c "\.from(" src/app/api/webhooks/square/catalog/route.ts
    Then: grep -c "\.eq('tenant_id'" src/app/api/webhooks/square/catalog/route.ts
    Expected: The tenant_id eq count should be >= the .from() count (every .from() has a corresponding tenant filter, inserts may add additional).
    Run: grep -c "\.from(" src/app/api/webhooks/square/inventory/route.ts
    Then: grep -c "\.eq('tenant_id'" src/app/api/webhooks/square/inventory/route.ts
    Expected: Same — tenant_id eq count >= .from() count.
  </verify>
  <done>
    Both webhook route files have .eq('tenant_id', tenantId) on every .from() query. TypeScript builds clean. No unfiltered service-role queries remain in either file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize KDS queries by tenantId</name>
  <files>src/lib/kds/queries.ts</files>
  <action>
    Add tenantId: string as the first parameter to every exported function that queries the database. Add .eq('tenant_id', tenantId) to every .from() query. Update internal callers within the same file.

    Functions to update (all exported):
    - getCategories(tenantId: string, screen?: KDSScreen)
    - getCategoryBySlug(tenantId: string, slug: string)
    - getMenuItems(tenantId: string, categoryId?: string)
    - getMenuItemsByScreen(tenantId: string, screen: KDSScreen)
    - getImages(tenantId: string, screen?: KDSScreen)
    - getSettings(tenantId: string)
    - getSetting(tenantId: string, key: K)
    - getCategoriesWithItems(tenantId: string, screen: KDSScreen) — update internal calls to getCategories(tenantId, screen) and getMenuItems(tenantId, category.id)
    - getScreenData(tenantId: string, screen: KDSScreen) — update internal calls to getCategoriesWithItems(tenantId, screen), getImages(tenantId, screen), getSettings(tenantId)
    - upsertCategory(tenantId: string, category: ...) — add tenant_id: tenantId to the upsert data object
    - upsertMenuItem(tenantId: string, item: ..., categorySlug: string) — update internal call to getCategoryBySlug(tenantId, categorySlug), add tenant_id: tenantId to the upsert data object
    - upsertImage(tenantId: string, image: ...) — add tenant_id: tenantId to the upsert data object
    - updateSetting(tenantId: string, key: K, value: ...) — add tenant_id: tenantId to the upsert data object
    - deleteAllMenuItems(tenantId: string) — add .eq('tenant_id', tenantId) before .neq('id', ...)
    - deleteAllCategories(tenantId: string) — add .eq('tenant_id', tenantId) before .neq('id', ...)

    For upsert operations: add tenant_id to the onConflict fields where slug/filename is the conflict key. The onConflict should remain 'slug' for categories, 'square_variation_id' for menu items, 'filename' for images, 'key' for settings. Simply adding tenant_id to the data is sufficient; the unique constraint enforcement happens at DB level.

    Important: do NOT change the function return types or the TypeScript generics on getSetting/updateSetting. Only add the tenantId parameter at the front of each signature and add the .eq() filter.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Should produce zero TypeScript errors in src/lib/kds/queries.ts.
    Run: grep -c "\.eq('tenant_id', tenantId)" src/lib/kds/queries.ts
    Should return 15 or more (one per exported function with a DB query).
  </verify>
  <done>
    All exported KDS query functions accept tenantId as first parameter. Every .from() query has .eq('tenant_id', tenantId). TypeScript builds clean. Callers of these functions are updated in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tenant_id to database.ts createOrder, fix admin/setup.ts, and update orders/route.ts caller</name>
  <files>
    src/lib/supabase/database.ts
    src/lib/admin/setup.ts
    src/app/api/orders/route.ts
  </files>
  <action>
    For src/lib/supabase/database.ts:
    The audit flagged createOrder() at line 71 which uses createServiceClient() without tenant_id. The function is called from customer-facing routes during checkout. Add tenantId: string as a required parameter to createOrder(). Include tenant_id: tenantId in the orderInsertData object so the orders row is tagged. Include tenant_id: tenantId in each item in itemsInsertData. The function signature becomes:
      export async function createOrder(orderData: { tenantId: string; userId?: string; ... })
    Pull tenantId from orderData.tenantId in the function body. Update the orderInsertData and itemsInsertData to include tenant_id.

    For src/lib/admin/setup.ts:
    This file has three functions that query profiles via service role: setUserAsAdmin(), isUserAdmin(), getUserRole(). These are legacy admin-by-email utilities from pre-multi-tenant era. The profiles table now uses tenant_memberships for admin checks. The correct fix is:
    1. Add a JSDoc comment to each function: "@deprecated Use tenant_memberships table for role checks. See requireAdmin() in src/lib/auth/admin.ts."
    2. Add tenantId: string parameter to isUserAdmin() and getUserRole(). Add .eq('tenant_id', tenantId) to the profiles queries in those two functions.
    3. The profiles table has a tenant_id column (from Phase 20 migration). Add tenantId: string parameter and .eq('tenant_id', tenantId) to setUserAsAdmin()'s update query as well.

    Do not change createUserProfile, getUserProfile, updateUserProfile, getOrdersForUser, updateOrderStatus — these use createClient() (user-scoped RLS client) and are correctly scoped.

    For src/app/api/orders/route.ts:
    This file calls createOrder(body) without tenantId. After the signature change above, it will fail to compile. Fix:
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In the POST handler, after auth/validation passes, add: `const tenantId = await getCurrentTenantId()`
    3. Spread tenantId into the createOrder call: `createOrder({ ...body, tenantId })`
    This ensures customer orders are tagged with the correct tenant.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Should produce zero TypeScript errors in all three files.
    Run: grep -n "tenant_id" src/lib/supabase/database.ts
    Should show tenant_id in createOrder's insert data for both orders and order_items.
    Run: grep -n "tenantId" src/lib/admin/setup.ts
    Should show tenantId parameter in all three functions.
    Run: grep -n "getCurrentTenantId\|tenantId" src/app/api/orders/route.ts
    Should show getCurrentTenantId import and tenantId passed to createOrder.
  </verify>
  <done>
    createOrder() accepts tenantId and stamps orders + order_items with tenant_id. All three admin/setup.ts functions accept tenantId and filter profile queries by tenant_id. api/orders/route.ts calls getCurrentTenantId() and passes tenantId to createOrder(). TypeScript builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update KDS caller files to pass tenantId to all kds/queries functions</name>
  <files>
    src/app/api/admin/kds/settings/route.ts
    src/app/admin/(kds)/layout.tsx
    src/app/kds/layout.tsx
    src/app/admin/(kds)/kds/food/page.tsx
    src/app/admin/(kds)/kds/drinks/page.tsx
    src/app/admin/(protected)/settings/page.tsx
  </files>
  <action>
    After Task 2 changes kds/queries.ts signatures to require tenantId as first argument, all 6 caller files will fail to compile. Update each caller to obtain and pass tenantId.

    Pattern for API routes (src/app/api/admin/kds/settings/route.ts):
    This file calls getSettings() and updateSetting(key, value) without tenantId. It is an admin API route that has access to the request object.
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In the GET handler: add `const tenantId = await getCurrentTenantId()` after auth check. Update call to `getSettings(tenantId)`.
    3. In the PATCH handler: add `const tenantId = await getCurrentTenantId()` after auth check. Update calls to `updateSetting(tenantId, 'theme', body.theme)` and `getSettings(tenantId)`.

    Pattern for Server Component layouts and pages:
    These are Next.js Server Components — they do NOT have access to the NextRequest object, but they CAN call `getCurrentTenantId()` from @/lib/tenant/context because that function reads from the Next.js cookies() store (available in Server Components).

    src/app/admin/(kds)/layout.tsx:
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In KDSLayout(): add `const tenantId = await getCurrentTenantId()` at the top of the function body.
    3. Update call from `getSetting('theme')` to `getSetting(tenantId, 'theme')`.

    src/app/kds/layout.tsx:
    Same pattern as admin kds layout.
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In KDSLayout(): add `const tenantId = await getCurrentTenantId()`.
    3. Update call from `getSetting('theme')` to `getSetting(tenantId, 'theme')`.

    src/app/admin/(kds)/kds/food/page.tsx:
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In FoodDisplayPage(): add `const tenantId = await getCurrentTenantId()`.
    3. Update call from `getScreenData('food')` to `getScreenData(tenantId, 'food')`.

    src/app/admin/(kds)/kds/drinks/page.tsx:
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'`
    2. In DrinksDisplayPage(): add `const tenantId = await getCurrentTenantId()`.
    3. Update call from `getScreenData('drinks')` to `getScreenData(tenantId, 'drinks')`.

    src/app/admin/(protected)/settings/page.tsx:
    This file imports getSettings as getKDSSettings from @/lib/kds/queries.
    1. Add import: `import { getCurrentTenantId } from '@/lib/tenant/context'` (or merge into existing tenant imports if any exist).
    2. In the page component: add `const tenantId = await getCurrentTenantId()`.
    3. Update the call from `getKDSSettings()` to `getKDSSettings(tenantId)`.

    Do NOT change any non-KDS-query logic in these files.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Expected: Zero TypeScript errors across all 6 files.
    Run: grep -n "getCurrentTenantId\|tenantId" src/app/api/admin/kds/settings/route.ts
    Expected: Shows getCurrentTenantId import and tenantId passed to getSettings/updateSetting.
    Run: grep -n "getCurrentTenantId\|tenantId" src/app/admin/(kds)/layout.tsx
    Expected: Shows getCurrentTenantId and tenantId passed to getSetting.
    Run: grep -n "getCurrentTenantId\|tenantId" src/app/kds/layout.tsx
    Expected: Shows getCurrentTenantId and tenantId passed to getSetting.
  </verify>
  <done>
    All 6 KDS caller files obtain tenantId via getCurrentTenantId() and pass it as first argument to every kds/queries function call. TypeScript builds clean. KDS data is now tenant-scoped end-to-end.
  </done>
</task>

</tasks>

<verification>
After completing all four tasks:
1. Run: npm run build
   Expected: Build succeeds with zero TypeScript errors.
2. Run: grep -c "\.from(" src/app/api/webhooks/square/catalog/route.ts
   Then: grep -c "\.eq('tenant_id'" src/app/api/webhooks/square/catalog/route.ts
   Expected: tenant_id eq count >= .from() count (every .from() has a corresponding tenant filter).
3. Run: grep -c "tenantId" src/lib/kds/queries.ts
   Expected: 16 or more matches (one per exported function signature).
4. Run: grep -n "getCurrentTenantId" src/app/api/orders/route.ts
   Expected: Shows import and usage.
5. Verify the audit script would now PASS these files:
   Run: bash audits/service-role-audit.sh 2>/dev/null | grep -A3 "webhook"
   Expected: Status shows improvement (note: script may still flag some due to pattern limitations, but visual review confirms fixes).
</verification>

<success_criteria>
- Both webhook routes pass tenant_id to every service-role DB query
- KDS queries.ts: all 15+ exported functions accept tenantId as first parameter
- database.ts: createOrder() includes tenant_id in orders and order_items inserts
- api/orders/route.ts: calls getCurrentTenantId() and spreads tenantId into createOrder call
- admin/setup.ts: all three profile functions accept tenantId parameter
- All 6 KDS caller files: pass tenantId to every kds/queries function
- npm run build produces zero TypeScript errors
- 5 files removed from FAIL category (webhooks x2, kds/queries, database, admin/setup) = audit shows ~59 FAIL instead of 64
</success_criteria>

<output>
After completion, create `.planning/phases/70-integration-testing-hardening/70-04-SUMMARY.md`
</output>
