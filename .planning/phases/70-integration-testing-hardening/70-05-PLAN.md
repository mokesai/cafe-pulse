---
phase: 70-integration-testing-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/siteSettings.edge.ts
  - src/lib/services/siteSettings.ts
  - middleware.ts
  - src/app/(site)/layout.tsx
  - src/app/admin/(protected)/settings/page.tsx
  - src/app/api/admin/settings/site/route.ts
  - src/app/under-construction/page.tsx
  - src/app/api/public/site-status/route.ts
autonomous: false

must_haves:
  truths:
    - "Maintenance mode can be set independently per tenant (Tenant A can be in maintenance while Tenant B is live)"
    - "The site status cache is keyed by tenantId so each tenant has an independent cached value"
    - "getCachedSiteStatus() accepts a tenantId parameter and returns status scoped to that tenant"
    - "invalidateSiteStatusCache() accepts an optional tenantId to invalidate one tenant or all tenants"
    - "middleware.ts reads the x-tenant-id cookie it just set and passes tenantId to getCachedSiteStatus()"
    - "All 5 siteSettings.ts callers are updated to pass tenantId"
  artifacts:
    - path: "src/lib/services/siteSettings.edge.ts"
      provides: "Per-tenant site status cache using Map<string, CacheEntry>"
      contains: "Map<string, CacheEntry>"
    - path: "src/lib/services/siteSettings.ts"
      provides: "saveSiteSettings with tenant_id filtering"
      contains: "tenant_id"
    - path: "middleware.ts"
      provides: "Maintenance mode check passing tenantId to getCachedSiteStatus"
      contains: "getCachedSiteStatus(request, tenantId)"
  key_links:
    - from: "src/lib/services/siteSettings.edge.ts"
      to: "getCachedSiteStatus callers (middleware)"
      via: "tenantId parameter added to function signature"
      pattern: "getCachedSiteStatus.*tenantId"
    - from: "src/lib/services/siteSettings.ts"
      to: "site_settings table"
      via: ".eq('tenant_id', tenantId) on all queries"
      pattern: "\\.eq\\('tenant_id'"
    - from: "middleware.ts"
      to: "getCachedSiteStatus"
      via: "x-tenant-id cookie read from sessionResponse.cookies after tenant resolution (line ~118)"
      pattern: "getCachedSiteStatus\\(request, tenantId\\)"
---

<objective>
Resolve the WARNING gap in the 70-02 security audit: the __siteStatusCacheEdge cache uses a singleton pattern (one cached value shared across all tenants) instead of a tenant-scoped Map. This plan makes the decision (per-tenant) and implements it, including updating all callers so the TypeScript build remains clean.

Purpose: Multi-tenant SaaS requires each tenant to independently control their own maintenance mode. A singleton cache means if Little Cafe goes into maintenance, any future second tenant would also appear in maintenance — wrong behavior. The site_settings table already has tenant_id from Phase 20; the cache just needs to be per-tenant.

Output: siteSettings.edge.ts refactored to Map<string, CacheEntry>; siteSettings.ts queries filtered by tenant_id; middleware.ts and all 5 siteSettings callers updated to pass tenantId.
</objective>

<execution_context>
@~/.gsd/workflows/execute-plan.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-integration-testing-hardening/70-VERIFICATION.md
@audits/AUDIT_RESULTS.md
@src/lib/services/siteSettings.edge.ts
@src/lib/services/siteSettings.ts
@src/lib/tenant/context.ts
@middleware.ts
@src/app/(site)/layout.tsx
@src/app/admin/(protected)/settings/page.tsx
@src/app/api/admin/settings/site/route.ts
@src/app/under-construction/page.tsx
@src/app/api/public/site-status/route.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Should maintenance mode (site status) be per-tenant or global?</decision>
  <context>
    The site_settings table has a tenant_id column (added in Phase 20 migration). The current siteSettings.edge.ts cache uses a singleton (one value for all requests). The current siteSettings.ts queries filter by id=1 (the original single-tenant row), not by tenant_id.

    Option A (per-tenant): Each tenant independently controls their maintenance mode. Tenant A can take their storefront offline without affecting Tenant B. Requires: refactor cache to Map keyed by tenantId, update siteSettings.ts to filter by tenant_id, update middleware to pass tenantId to getCachedSiteStatus.

    Option B (global/platform): One maintenance mode setting that affects all tenants simultaneously. Useful if the entire platform goes down (infrastructure maintenance). Simpler — keep singleton cache, document it. Platform admin controls the single setting.

    Context: This is the Little Cafe at Kaiser Permanente. The roadmap shows we're building toward multi-tenant SaaS. The site_settings table already has tenant_id. The natural SaaS behavior is per-tenant maintenance mode.

    Recommendation: Option A (per-tenant). This is a multi-tenant SaaS platform. Each cafe should be able to independently manage their storefront availability.
  </context>
  <options>
    <option id="option-a">
      <name>Per-tenant maintenance mode</name>
      <pros>Each tenant independently controls availability; correct SaaS model; site_settings.tenant_id column is used properly</pros>
      <cons>Slightly more complex cache; middleware must pass tenantId to getCachedSiteStatus</cons>
    </option>
    <option id="option-b">
      <name>Global/platform maintenance mode</name>
      <pros>Simpler; useful for infrastructure-level outages; no cache refactor needed</pros>
      <cons>Wrong for SaaS — one tenant's maintenance affects all tenants; contradicts multi-tenant design; wastes the tenant_id column</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement per-tenant site status cache and update all callers (if option-a selected)</name>
  <files>
    src/lib/services/siteSettings.edge.ts
    src/lib/services/siteSettings.ts
    middleware.ts
    src/app/(site)/layout.tsx
    src/app/admin/(protected)/settings/page.tsx
    src/app/api/admin/settings/site/route.ts
    src/app/under-construction/page.tsx
    src/app/api/public/site-status/route.ts
  </files>
  <action>
    If option-b was selected: Add a comment to siteSettings.edge.ts explaining the intentional global design and skip the rest of this task.

    If option-a was selected (expected):

    --- siteSettings.edge.ts ---

    1. Change the global declaration from:
         var __siteStatusCacheEdge: CacheEntry | undefined
       to:
         var __siteStatusCacheEdge: Map<string, CacheEntry> | undefined
    2. Add a helper function to get or initialize the cache Map:
         function getCache(): Map<string, CacheEntry> {
           if (!globalThis.__siteStatusCacheEdge) {
             globalThis.__siteStatusCacheEdge = new Map()
           }
           return globalThis.__siteStatusCacheEdge
         }
    3. Change fetchSiteStatus() to accept tenantId: string as a second parameter. When constructing the statusUrl, append ?tenantId=${tenantId} as a query parameter so the /api/public/site-status API route knows which tenant to query:
         async function fetchSiteStatus(request: NextRequest, tenantId: string): Promise<SiteStatus>
         const statusUrl = new URL('/api/public/site-status', request.url)
         statusUrl.searchParams.set('tenantId', tenantId)
    4. Change getCachedSiteStatus() signature to:
         export async function getCachedSiteStatus(request: NextRequest, tenantId: string, forceRefresh = false): Promise<SiteStatus>
       Update the body to use the Map:
         const cache = getCache()
         const cached = cache.get(tenantId)
         if (!forceRefresh && cached && cached.expiresAt > now) {
           return cached.status
         }
         const status = await fetchSiteStatus(request, tenantId)
         cache.set(tenantId, { status, expiresAt: now + CACHE_TTL_MS })
         return status
    5. Change invalidateSiteStatusCache() to accept optional tenantId:
         export function invalidateSiteStatusCache(tenantId?: string) {
           if (tenantId) {
             getCache().delete(tenantId)
           } else {
             globalThis.__siteStatusCacheEdge = undefined
           }
         }

    --- siteSettings.ts ---

    1. Change getSiteSettings() to accept tenantId: string. Replace .eq('id', 1) with .eq('tenant_id', tenantId). Function signature: export async function getSiteSettings(tenantId: string): Promise<SiteSettings | null>
    2. Change getSiteStatus() to accept tenantId: string and pass to getSiteSettings(tenantId).
    3. Change saveSiteSettings() to accept tenantId: string (add as first parameter before payload). Replace .eq('id', 1) with .eq('tenant_id', tenantId) on the SELECT and UPDATE queries. Update the INSERT data to include tenant_id: tenantId (remove the hardcoded id: 1 or replace with tenant_id).
    4. Change getSiteStatusUsingServiceClient() to accept tenantId: string. Replace .eq('id', 1) with .eq('tenant_id', tenantId).
    5. In invalidateEdgeCache() (the private function): change the function to accept tenantId: string and pass it through: invalidateEdgeCache(tenantId: string) calls invalidateSiteStatusCache(tenantId).
    6. Update saveSiteSettings to call invalidateEdgeCache(tenantId) instead of invalidateEdgeCache().

    --- middleware.ts ---

    middleware.ts is NOT an admin route and is not covered by plans 70-06 or 70-07. It must be fixed here.

    The middleware already resolves the tenant at line ~111-149 (sets x-tenant-id cookie on sessionResponse). By line ~151, sessionResponse has the x-tenant-id cookie set. Read it to get tenantId for the maintenance check:

    In the maintenance mode check block (around line 156):
    1. Before calling getCachedSiteStatus, read the tenant ID from the cookie that was just set on sessionResponse:
         const tenantId = sessionResponse.cookies.get('x-tenant-id')?.value
                          ?? request.cookies.get('x-tenant-id')?.value
                          ?? DEFAULT_TENANT_ID
       (First try the just-set cookie on sessionResponse, fall back to request cookie, fall back to DEFAULT_TENANT_ID.)
    2. Update the call: `const status = await getCachedSiteStatus(request, tenantId)`

    This is the correct approach because:
    - middleware.ts cannot use the cookies() helper from next/headers (that's for Server Components/API routes)
    - The tenant cookie was just set on sessionResponse a few lines earlier
    - Falling back to request.cookies handles requests where the cookie was set in a prior request

    --- Caller files: read each before editing ---

    src/app/(site)/layout.tsx:
    Read this file first. Find any call to getSiteSettings() or getSiteStatus() and add tenantId.
    Obtain tenantId via: `import { getCurrentTenantId } from '@/lib/tenant/context'` then `const tenantId = await getCurrentTenantId()`.

    src/app/admin/(protected)/settings/page.tsx:
    Read this file first. Find calls to getSiteSettings() or getKDSSettings() from siteSettings.ts and add tenantId.
    Note: this file also calls getKDSSettings from kds/queries.ts — that is handled by plan 70-04. Focus only on siteSettings.ts callers here.
    Obtain tenantId via getCurrentTenantId() from @/lib/tenant/context.

    src/app/api/admin/settings/site/route.ts:
    Read this file first. Find calls to getSiteSettings(), saveSiteSettings(), or getSiteStatusUsingServiceClient() and add tenantId.
    This is an API route — use getCurrentTenantId() from @/lib/tenant/context (it reads from Next.js cookies store).

    src/app/under-construction/page.tsx:
    Read this file first. Find calls to getSiteSettings() or getSiteStatus() and add tenantId.
    Obtain tenantId via getCurrentTenantId() from @/lib/tenant/context.

    src/app/api/public/site-status/route.ts:
    Read this file first. This route is called BY getCachedSiteStatus (it fetches site status). After the signature change, it needs to accept a tenantId query parameter and pass it to getSiteStatusUsingServiceClient(tenantId).
    Update: read tenantId from URL searchParams (`url.searchParams.get('tenantId') ?? DEFAULT_TENANT_ID`) and pass to getSiteStatusUsingServiceClient(tenantId).
    Import DEFAULT_TENANT_ID from '@/lib/tenant/types' if not already imported.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Expected: Zero TypeScript errors in siteSettings.edge.ts, siteSettings.ts, middleware.ts, and all 5 caller files.
    Run: grep "Map<string, CacheEntry>" src/lib/services/siteSettings.edge.ts
    Expected: Returns the Map type declaration line.
    Run: grep -n "\.eq('tenant_id'" src/lib/services/siteSettings.ts
    Expected: Returns 3+ matches (one per query).
    Run: grep -n "getCachedSiteStatus" middleware.ts
    Expected: Shows getCachedSiteStatus(request, tenantId) — both arguments present.
  </verify>
  <done>
    siteSettings.edge.ts uses Map<string, CacheEntry> keyed by tenantId. getCachedSiteStatus() and invalidateSiteStatusCache() accept tenantId. siteSettings.ts functions accept tenantId and filter by tenant_id. middleware.ts reads tenantId from the cookie and passes it to getCachedSiteStatus(). All 5 caller files updated. TypeScript builds clean.
  </done>
</task>

</tasks>

<verification>
After completion:
1. Run: npm run build
   Expected: Zero TypeScript errors.
2. Confirm the cache architecture decision is recorded:
   Run: grep "Map<string" src/lib/services/siteSettings.edge.ts
   Expected: Shows Map-based cache.
3. Confirm siteSettings.ts no longer uses .eq('id', 1):
   Run: grep "eq('id', 1)" src/lib/services/siteSettings.ts
   Expected: No matches.
4. Confirm middleware passes tenantId:
   Run: grep "getCachedSiteStatus" middleware.ts
   Expected: Shows two arguments — request and tenantId.
5. Update STATE.md with the decision: "Site status cache is per-tenant (Map<string, CacheEntry> keyed by tenantId) — each tenant independently controls maintenance mode."
</verification>

<success_criteria>
- Decision made: per-tenant maintenance mode (option-a)
- siteSettings.edge.ts: __siteStatusCacheEdge is Map<string, CacheEntry>
- getCachedSiteStatus(request, tenantId, forceRefresh?) signature in place
- siteSettings.ts: all queries use .eq('tenant_id', tenantId) instead of .eq('id', 1)
- middleware.ts: reads x-tenant-id from sessionResponse.cookies and passes to getCachedSiteStatus(request, tenantId)
- All 5 siteSettings caller files updated to pass tenantId
- npm run build produces zero TypeScript errors
- VERIFICATION.md gap "All globalThis caches use tenant-scoped keys" can be re-verified as PASS
</success_criteria>

<output>
After completion, create `.planning/phases/70-integration-testing-hardening/70-05-SUMMARY.md`
</output>
